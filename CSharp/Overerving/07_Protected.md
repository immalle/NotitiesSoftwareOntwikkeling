# Protected instantievariabelen

Class-variabelen kunnen naast `private` of `public` ook `protected` zijn.

Dit is nuttig omdat dan enkel de class zelf en **overervende classes** toegang
hebben.

Zie ook hoofdstuk 10.

